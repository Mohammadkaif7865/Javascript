const person = {
        name: "Fred",
        age: 56
    }
    //... can spread na array on another and can also oject in another object;
const sPerson = {...person };
console.log(person);
console.log(sPerson);